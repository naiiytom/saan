[package]
name = "saan_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
saan_core = { path = "../saan_core" }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
