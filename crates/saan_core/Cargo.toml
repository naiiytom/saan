[package]
name = "saan_core"
version = "0.1.0"
edition = "2024"

[dependencies]
duckdb = { version = "1", features = ["bundled"] }
petgraph = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlparser = "0.50"
thiserror = "1"
walkdir = "2"

[dev-dependencies]
tempfile = "3"
